<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.oneteaapp">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TranslucentStatusTheme">
        <activity android:name=".activity.PerfectOrderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LogingActivity" />
        <activity android:name=".StartActivity">
        </activity>
        <activity android:name=".activity.ShoppingCartActivity" />
        <activity android:name=".view.flowlayout.ScanActivity" />
        <activity android:name=".activity.HomeScanActivity" />
        <activity android:name=".activity.CommoditDetailsyActivity" />
        <activity android:name=".activity.ClassifyParticularsActivity" />
        <activity android:name=".activity.QRCodeActivity" />
        <activity android:name=".activity.BaseActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".wxapi.WXPayEntryActivity"
            android:exported="true"
            android:launchMode="singleTop" />
    </application>

</manifest>